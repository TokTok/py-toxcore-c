language: python
sudo: false

cache:
  directories:
    - $HOME/cache

python:
#  - "2.6"
  - "2.7"
#  - "3.2"
#  - "3.3"
#  - "3.4"
#  - "3.5"

compiler:
#  - gcc
  - clang

env:
  - FORK=iphydf BRANCH=logger

addons: {apt: {packages: [yasm]}}

before_script:
  - export CFLAGS="-I$HOME/cache/usr/include"
  - export LDFLAGS="-L$HOME/cache/usr/lib"
  - export LD_LIBRARY_PATH="$HOME/cache/usr/lib"
  - export PKG_CONFIG_PATH="$HOME/cache/usr/lib/pkgconfig"
  - tools/install-deps.sh > /dev/null

script:
  - pip install .
  - python tests/tests.py
